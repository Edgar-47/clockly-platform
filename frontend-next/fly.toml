# Fly.io configuration for ClockLy frontend (Next.js).
# Deploy from the frontend-next/ directory:
#   cd frontend-next && fly deploy --app clockly-app

app    = "clockly-app"
primary_region = "mad"   # Madrid

[build]
  dockerfile = "Dockerfile"

  # These build args are baked into the Next.js routes manifest at build time.
  # API_URL_INTERNAL: server-to-server proxy destination for Next.js rewrites.
  #   Uses the public API URL — the Fly.io 6PN internal network (clockly-api.internal:8000)
  #   was unreachable from the frontend machine (IPv6 private networking not connecting).
  #   Public URL adds minor TLS overhead but is reliable and already proven in smoke tests.
  # NEXT_PUBLIC_API_URL: public API origin — baked into client JS and CSP.
  [build.args]
    API_URL_INTERNAL    = "https://api.clockly.es"
    NEXT_PUBLIC_API_URL = "https://api.clockly.es"
    NEXT_PUBLIC_APP_NAME = "ClockLy"

# ── Non-sensitive runtime env ─────────────────────────────────────────────────
[env]
  NODE_ENV               = "production"
  NEXT_TELEMETRY_DISABLED = "1"

# ── HTTP service ──────────────────────────────────────────────────────────────
[http_service]
  internal_port        = 3000
  force_https          = true
  auto_stop_machines   = "stop"
  auto_start_machines  = true
  min_machines_running = 1
  processes            = ["app"]

  [[http_service.checks]]
    grace_period = "15s"
    interval     = "30s"
    method       = "GET"
    path         = "/api/health"
    protocol     = "http"
    timeout      = "10s"

# ── VM sizing ─────────────────────────────────────────────────────────────────
[[vm]]
  memory   = "512mb"
  cpu_kind = "shared"
  cpus     = 1
